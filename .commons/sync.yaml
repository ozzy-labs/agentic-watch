# Auto-updated by commons sync.sh
# 'pinned' is user-editable — add or remove paths freely
commit: f628f1f046786fc08775e4691d6840f0710123fc
synced_at: 2026-05-18T00:47:11Z

# Skills sync (opt-in). Add adapter ids to skills_adapters and a SHA
# to skills_commit. The skills repo's main HEAD SHA can be obtained via:
#   gh api repos/ozzy-labs/skills/commits/main --jq .sha
skills_commit: 27ae96210fee135e608b16e938cb338c9b2b45ae
skills_adapters:
  - claude-code
  - codex-cli
  - gemini-cli
  - copilot

# Files excluded from commons sync. Preserves local overrides that diverge
# from the commons baseline (PR-specific decisions, incident remediations,
# version migrations).
pinned:
  # PR #91 / #72: keep IDE/coverage/worktree/temp ignores; commons drops them
  - .gitignore
  # PR #127: biome pre-commit hook (CI breakage remediation, see file header)
  - lefthook.yaml
  # PR #128: biome schema migrated to 2.4.13; commons still on 2.4.9
  - biome.json
  # release-please generates CHANGELOG.md with style that doesn't match MD012
  - .markdownlint-cli2.yaml
  # PR #344 / #348: routine docs carry feedradar-specific `radar routine`
  # content (generate / --prompt-mode bootstrap / fire / delete_branch_on_merge
  # / custom network_access security guidance) absent from the generic commons
  # baseline. Pin so commons sync (e.g. #351) does not revert them.
  - .claude/routines/README.md
  - .claude/routines/_template.yaml
