prompt = "Run `agentic-watch review {{args}}` to cross-review the specified research report. Surface the resulting file path and any errors from stdout/stderr. The canonical procedure lives in `.agents/skills/review/SKILL.md` (SSoT); this command is a thin wrapper that delegates to the CLI."
description = "Cross-review an existing research report via agentic-watch."
